apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "report-generator.fullname" . }}-config
  labels:
    {{- include "report-generator.labels" . | nindent 4 }}
data:
  FEATURE: report-generator
