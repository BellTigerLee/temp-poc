apiVersion: v1
kind: Service
metadata:
  name: {{ include "temp-poc.fullname" . }}-report-generator
  labels:
    {{- include "temp-poc.labels" . | nindent 4 }}
    app.kubernetes.io/component: report-generator
  annotations:
    {{- toYaml .Values.reportGenerator.service.annotations | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "temp-poc.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: report-generator
  ports:
    - name: http
      port: {{ .Values.reportGenerator.service.port }}
      targetPort: http
